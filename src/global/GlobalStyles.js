import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
    mt10ForContainer:{
        marginTop:10,
    },
    whiteBoldText:{
        color:'white',
        fontWeight:'bold',
    }
});
